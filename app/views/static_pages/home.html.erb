<% if signed_in? %> <!-- The user is signed in -->
	<%= render partial: 'shared/home_signed_in' %>
<% else %> <!-- The user is not signed in -->
	<%= render partial: 'shared/home_not_signed_in' %>
<% end %>